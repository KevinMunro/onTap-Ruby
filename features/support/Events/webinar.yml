webinar:
  select_event_style: Webinar
  set_event_title: Generate
  set_event_description: Generate
  set_event_url_field: Generate
  set_event_host_field: Generate
  set_date: Generate
  set_start_time: Generate
  set_end_time: Generate
